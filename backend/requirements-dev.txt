# Test- und Entwicklungs-Abhängigkeiten
# Werden NICHT in das Production-Image installiert
-r requirements.txt

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
httpx==0.28.1
respx==0.22.0
fakeredis==2.26.2

# Linting / Formatting
ruff==0.8.4
mypy==1.13.0
